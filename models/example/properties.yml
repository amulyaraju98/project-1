sources:
  - name: "Amulya_sources"
    database: db_ecom
    schema: sc_ecom
    tables:
      - name: Raw_users
      - name: Raw_country
      - name: Raw_Sales
      - name: shipping
      - name: currency
    

models:
  - name: 'src_users'
    config:
      materialized: 'table'
  - name: 'learning_macro'
    config:
      materialized: 'table'
  - name: 'new_currency'
    config:
      materialized: 'table'
